import styled from 'styled-components/native'

import UiViewPropsType from '@ts/ui/UiViewProps.type'

const SelectRoot = styled.View.attrs((props) => ({}))<UiViewPropsType>``

export default SelectRoot
